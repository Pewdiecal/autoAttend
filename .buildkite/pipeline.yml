steps:
  - label: ":hammer: Build"
    command: "gem install bundle && bundle exec fastlane testBuild"
    plugins:
      - docker-compose#v3.7.0:
          mount-buildkite-agent: true
          run: app
    agents:
      queue: "android"

